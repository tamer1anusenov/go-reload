1. COMMAND WORD TRANSFORMATIONS
bin(low) - should lowercase "bin"
BIN(low) - should lowercase "bin"
hex(up) - should uppercase "hex"
HEX(up) - should uppercase "hex"
cap(cap) - should capitalize "cap"
CAP(cap) - should capitalize "cap"
up(low) - should lowercase "up"
UP(low) - should lowercase "up"
low(up) - should uppercase "low"
LOW(up) - should uppercase "low"

2. COMMAND WORD FOLLOWED BY NUMBERS
01010010101010101010100101010 (BIN(low))
01010010101010101010100101010 (bin)

AB11 (HEX(low))
AB11 (hex)

3. MULTI-LEVEL NESTING
cap(up(low))
CAP(up(low))
cap(UP(low))
CAP(UP(LOW))

4. EDGE CASES
BIN(invalid) - should not transform
hex(unknown) - should not transform
(bin)(low) - should process separately
(hex)(up) - should process separately 